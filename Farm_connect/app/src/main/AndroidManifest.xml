<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.farm_connect">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Farm_connect">
        <activity android:name=".WebActivity"></activity>
        <activity android:name=".Profile" />
        <activity android:name=".Discussion" />
        <activity android:name=".MandiPrice" />
        <activity android:name=".UserDashboard" />
        <activity android:name=".verifyenterotptwo" />
        <activity android:name=".LoginwithEmail" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginType" />
        <activity android:name=".Register" />
        <activity android:name=".PhoneLogin" />
        <activity android:name=".PostAnswerActivity"/>
        <activity android:name=".PostQuestionActivity"/>
        <service
            android:name=".MyFirebaseMessagingService"
            android:exported="false"
            android:permission="TODO"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
    </application>

</manifest>